# Copy this file to `.env.local` (gitignored) and fill in real values
# before building/running the app. `.env.local` provides default POC
# login credentials used by SessionBootstrapper for auto-login at startup.
DEFAULT_EMAIL=changeme@example.com
DEFAULT_PASSWORD=changeme
