name: syncos_screen
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  bloc: ^9.2.1
  collection: ^1.19.1
  dio: ^5.9.2
  equatable: ^2.0.8
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  firebase_core: ^4.11.0
  firebase_database: ^12.4.4
  flutter_bloc: ^9.1.1
  flutter_dotenv: ^6.0.1
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^10.2.0
  flutter_svg: ^2.3.0
  go_router: ^17.3.0
  intl: ^0.20.2
  devices:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/devices
      ref: staging
  networking:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/networking
      ref: staging
  device_manager:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/device_manager
      ref: staging
  power_clamp_device_history:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/power_clamp_device_history
      ref: staging
  design_system:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/design_system
      ref: staging
  auth:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/auth
      ref: staging

dependency_overrides:
  devices:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/devices
      ref: staging
  networking:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/networking
      ref: staging
  device_manager:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/device_manager
      ref: staging
  power_clamp_device_history:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/power_clamp_device_history
      ref: staging
  design_system:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/design_system
      ref: staging
  auth:
    git:
      url: https://github.com/SyncrowIOT/frontend-microservices.git
      path: packages/auth
      ref: staging

dev_dependencies:
  bloc_lint: ^0.4.1
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.3.0
  mockito: ^5.7.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/icons/
    - env/.env.development
    - env/.env.staging
    - env/.env.production
    - env/.env.local.development
    - env/.env.local.staging
    - env/.env.local.production
